package learning.database;

public class Index {
	/*
	 *  How to choose Database
	 * 		1. Structure of Data
	 * 		2. Volume
	 * 		3. How you query
	 * 
	 *  
	 *  Structured Data + ACID -> RDDBMS (MySQL, ORacle, Postgres, SQL Server). Ex :user, employee data
	 *  Non - Structured Data + Different characteristics of entity + Complex queries -> Document DB (Mongo DB, Couch Base). Ex: ecommerce with t-shirt, mobile,fridge,..
	 *  Non - Structured Data + Ever Increasing Data +finite (less) queries ->  Columnar DB (Cassandra, HBase) Ex: Uber driver sending locations
	 * 
	 */

}
