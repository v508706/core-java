package learning.BigONotation;

public class Index {

	/**
	 * Time Complexity :: 
	 * We always need to check worst case time complexity 
	 * O(n) <=> O(n),O(2n),O(n+1)  Ex: iteration  , factorial
	 * O(n^2)<=>O(n square), O(n square .anyContant) Ex: Iteration of same size array
	 * O(a.b) => Ex:iteration of different size array 
	 * O(2^n) =>  Ex: Fibonnaci Series
	 */
	
	
	/**Array operations with index = O(1) => constant time 
	 * Array operations without index = O(n) => Linear time
	 */
		
}
